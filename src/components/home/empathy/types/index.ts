export interface EmpathyData {
  title: string;
}

export interface EmpathyStyles {
  container: {
    base: string;
  };
  content: {
    base: string;
  };
  image: {
    base: string;
  };
  list: {
    base: string;
  };
  title: {
    base: string;
  };
  background: {
    base: string;
  };
}
