export type Catchphrase =
  | "深層筋ほぐしから"
  | "自律神経を整える"
  | "整体サロン";

export interface HeroStyles {
  container: {
    base: string;
  };
  desktop: {
    base: string;
  };
  mobile: {
    base: string;
  };
}
