import { StickyNavbar } from 'src/components/elements/nav/StickyNavbar';

export const Header = () => {
  return (
    <header className="sticky top-0 z-50">
      <StickyNavbar />
    </header>
  );
};
